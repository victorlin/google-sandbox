{
  "name": "google-sandbox",
  "productName": "Google Sandbox",
  "version": "0.1.0",
  "main": "build/App.js",
  "engines": {
    "node": "^20",
    "npm": "^10"
  },
  "scripts": {
    "build": "tsc && cp new-window.html build && cp preload.js build && cp config.json build",
    "start": "npm run build && electron-forge start",
    "make": "npm run build && electron-forge make"
  },
  "author": "Victor Lin",
  "license": "Apache-2.0",
  "devDependencies": {
    "@electron-forge/cli": "^7.8.3",
    "@electron-forge/maker-dmg": "^7.8.1",
    "electron": "^37.2.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
