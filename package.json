{
  "name": "google-sandbox",
  "productName": "Google Sandbox",
  "version": "0.1.0",
  "main": "build/App.js",
  "engines": {
    "node": "^24",
    "npm": "^11"
  },
  "scripts": {
    "build": "tsc && cp start.html build && cp preload.js build && cp config.json build",
    "start": "npm run build && electron-forge start",
    "make": "npm run build && electron-forge make"
  },
  "author": "Victor Lin",
  "license": "Apache-2.0",
  "devDependencies": {
    "@electron-forge/cli": "^7.10.2",
    "@electron-forge/maker-dmg": "^7.9.0",
    "electron": "^39.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
